PKG_CPPFLAGS = -D BSON_COMPILATION -DMONGOC_COMPILATION -DBSON_DISABLE_CHECKS -I. -Iyajl -Ibson -Imongoc
PKG_CFLAGS = -include apple.h

SOURCES = $(wildcard yajl/*.c bson/*.c mongoc/*.c *.c)

OBJECTS = $(SOURCES:.c=.o)

PKG_LIBS = -lssl -lcrypto -lsasl2

all: $(OBJECTS)

clean:
	rm -f $(OBJECTS)
