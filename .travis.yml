language: r

warnings_are_errors: true
#r_check_revdep: true

apt_packages:
  - libssl-dev
  - libsasl2-dev

r_github_packages:
  - jeroenooms/jsonlite

before_install:
  - echo "MAKE=${MAKE} -j8" >> /etc/R/Renviron.site

notifications:
  email:
    on_success: change
    on_failure: change
